# override
Класс доступный в глобальном пространстве любой сцены.

Методы:
## override:loadScene(string path)
Принудительно останавливает и выгружает текущую сцену и запускает сцену по пути path
```lua
override:loadScene("game.lua")
```
